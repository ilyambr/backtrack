<Window x:Class="Backtrack.RecentClipsOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Backtrack Recent Clips"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Visibility="Hidden">
    <!--
      Same "own top-level window" reasoning as StreamingStatusOverlay: MainWindow
      itself is AllowsTransparency="False" (needed for the VLC video surface),
      so anything wanting a genuinely separate floating box has to live here
      instead. Unlike StreamingStatusOverlay/LogoOverlay (passive, click-through,
      auto-positioned), this one is interactive on purpose: draggable via the
      grip on the left, and each tile is clickable/right-clickable, so it does
      NOT get ClickThrough.Enable in the code-behind.
    -->
    <Border Background="{DynamicResource PanelBg}" BorderBrush="{DynamicResource Hairline}" BorderThickness="1" Padding="8">
        <StackPanel Orientation="Horizontal">
            <!-- Six-dot grip, the standard "this is draggable" affordance. MouseLeftButtonDown
                 here calls DragMove() in the code-behind; nothing else on this window drags,
                 only the grip, so accidentally clicking a tile never also moves the window. -->
            <Border x:Name="DragHandle" Width="18" Cursor="SizeAll" Background="Transparent"
                    MouseLeftButtonDown="DragHandle_MouseLeftButtonDown" ToolTip="Drag to move">
                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Ellipse Grid.Row="0" Grid.Column="0" Width="3" Height="3" Fill="{DynamicResource Text2}" Margin="1.5"/>
                    <Ellipse Grid.Row="0" Grid.Column="1" Width="3" Height="3" Fill="{DynamicResource Text2}" Margin="1.5"/>
                    <Ellipse Grid.Row="1" Grid.Column="0" Width="3" Height="3" Fill="{DynamicResource Text2}" Margin="1.5"/>
                    <Ellipse Grid.Row="1" Grid.Column="1" Width="3" Height="3" Fill="{DynamicResource Text2}" Margin="1.5"/>
                    <Ellipse Grid.Row="2" Grid.Column="0" Width="3" Height="3" Fill="{DynamicResource Text2}" Margin="1.5"/>
                    <Ellipse Grid.Row="2" Grid.Column="1" Width="3" Height="3" Fill="{DynamicResource Text2}" Margin="1.5"/>
                </Grid>
            </Border>
            <StackPanel x:Name="TilesPanel" Orientation="Horizontal" Margin="8,0,0,0"/>
        </StackPanel>
    </Border>
</Window>
