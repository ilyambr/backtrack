<Window x:Class="Backtrack.OverlayLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Backtrack Log"
        Width="480"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Visibility="Hidden"
        Opacity="0">
    
    <Border Background="{DynamicResource PanelBgOpaque}" BorderBrush="{DynamicResource Hairline}" BorderThickness="1">
        <StackPanel>
            
            <TextBlock x:Name="ObsModeText" Text="" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource Text0}"
                       Padding="8,5" TextTrimming="CharacterEllipsis" Visibility="Collapsed"/>

            <ScrollViewer x:Name="BacktrackModeScroll" MaxHeight="62" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                <ItemsControl x:Name="BacktrackModeList" Padding="8,5">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="10.5" Foreground="{DynamicResource Text0}"
                                       TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="0,0,0,2"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Border>
</Window>
