<Window x:Class="Backtrack.StreamingStatusOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Backtrack Streaming Status"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Visibility="Hidden">
    
    <Border Background="{DynamicResource PanelBg}" BorderBrush="{DynamicResource Hairline}" BorderThickness="1" Padding="10,7">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Ellipse Width="8" Height="8" Fill="{DynamicResource Stream}" VerticalAlignment="Center" Margin="0,0,7,0"/>
            <TextBlock Text="You are currently livestreaming." FontSize="11.5" FontWeight="Bold"
                       Foreground="{DynamicResource Text1}" VerticalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
