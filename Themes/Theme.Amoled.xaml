<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
      True #000000 panel background, not just "very dark grey"; on an OLED
      panel those pixels are actually off (real power savings, deepest
      possible black), and #000000 also sidesteps the near-black color-crush
      issue #080808 ran into on some monitors, since 0,0,0 is the one value
      every display's gamma curve treats as a fixed calibration point rather
      than an interpolated one. High-contrast pure white text to match.

      Otherwise the same structure/steps as Theme.Dark.xaml, just anchored
      to 0 instead of 8; Rec/RecDark/Stream/Green stay Backtrack's own brand
      colors, same as every other theme.
    -->
    <SolidColorBrush x:Key="PanelBg" Color="#000000"/>
    <SolidColorBrush x:Key="PanelBgOpaque" Color="#EC000000"/>
    <!-- Recessed card surface; see Theme.Dark.xaml's own comment on this key.
         Can't go darker than PanelBg's true black, so this is lighter
         instead: a dark grey a card can actually be told apart from the
         page behind it. -->
    <SolidColorBrush x:Key="ThumbnailBg" Color="#161616"/>
    <SolidColorBrush x:Key="Hairline" Color="#1FFFFFFF"/>
    <SolidColorBrush x:Key="Text0" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="Text1" Color="#B0B0B0"/>
    <SolidColorBrush x:Key="Text2" Color="#757575"/>
    <SolidColorBrush x:Key="Accent" Color="#FFFFFF"/>

    <SolidColorBrush x:Key="Rec" Color="#FF5B52"/>
    <SolidColorBrush x:Key="RecDark" Color="#7A1F1F"/>
    <SolidColorBrush x:Key="Stream" Color="#A855F7"/>
    <SolidColorBrush x:Key="Green" Color="#3ECF8E"/>
    <!-- See Theme.Dark.xaml's own comment on this key: a fixed brand blue,
         same reasoning as Rec/Stream/Green, not derived from Accent. -->
    <SolidColorBrush x:Key="NewestClip" Color="#3B82F6"/>

    <SolidColorBrush x:Key="RowBg" Color="#0BFFFFFF"/>
    <SolidColorBrush x:Key="RowHoverBg" Color="#17FFFFFF"/>
    <SolidColorBrush x:Key="TileHoverBg" Color="#0FFFFFFF"/>
    <SolidColorBrush x:Key="BorderSubtle" Color="#14FFFFFF"/>
    <SolidColorBrush x:Key="BorderMedium" Color="#26FFFFFF"/>
    <SolidColorBrush x:Key="BorderStrong" Color="#33FFFFFF"/>
    <SolidColorBrush x:Key="SeekTrackBg" Color="#33FFFFFF"/>
    <SolidColorBrush x:Key="SeekTrackBuffer" Color="#66FFFFFF"/>

    <SolidColorBrush x:Key="BadgeBg" Color="#CC000000"/>
    <SolidColorBrush x:Key="BadgeBorder" Color="#14FFFFFF"/>
</ResourceDictionary>
