<Window x:Class="CaptureCenter.ToastOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Capture Center Notifications"
        Width="280"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="Height">
    <!--
      Always-on, left side of the screen, independent of MainWindow; same
      reasoning as StatusOverlay, notifications about what just happened
      shouldn't disappear just because the hotkey HUD is hidden.
    -->
    <StackPanel x:Name="ToastStack" Margin="0,0,0,0"/>
</Window>
